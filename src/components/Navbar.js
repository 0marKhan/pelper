import React from "react";
import styled from "styled-components";

const StyledNavbar = styled.div`
  background-color: #ff00ff;
  color: #fff;
  font-weight: bold;
  font-size: 1.5rem;
  padding: 1rem;
`;

const Navbar = () => {
  return <StyledNavbar>Pelper</StyledNavbar>;
};

export default Navbar;
